{% extends 'layout.njk' %}

{% block content%}

{% block head %}
<title>Sobre - {{aboutData.name}}</title>
{% endblock %}

    <div id="wrapper">
        <img src=" {{ aboutData.avatarUrl }} " alt="Renato Rodrigues">
        <h1>{{ abouData.name }}</h1>
        <h2>{{ aboutData.hole }}</h2>
        <p>{{ aboutData.description }}</p>
    </div>


    <div id="links-footer" class="links"> 

        {% for link in aboutData.links %}

        <a href="{{ link.url }}" target="_blank"> {{ link.social }} </a>

        {% endfor %}
    </div>
    
{% endblock %}